version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: unique-openbooks_server_1
      APP_PORT: 80

  server:
    image: evanbuss/openbooks:latest
    volumes:
      - ${APP_DATA_DIR}/data:/books/books
    command: --name $RM_NAME --persist
    restart: on-failure
